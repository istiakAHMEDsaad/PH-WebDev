/* const myObject = {
    value1: 10,
    value2: 20,
    value3: 30,
  };
  
  let total = 0;
  
  for (let key in myObject) {
    if (myObject.hasOwnProperty(key)) {
      total += myObject[key];
    }
  }
  
  console.log("The sum of values is:", total); */

function checkSum() {}
